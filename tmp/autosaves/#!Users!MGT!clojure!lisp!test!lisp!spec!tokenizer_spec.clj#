(ns lisp.tokenizer-spec
  (:use lisp.tokenizer speclj.core))

(describe
 "consume"
 (let [coll ["a" "b" "c" "d" "e"]]
   (it )))

(describe
 "tokenizer"

 (it "tokenizes a number"
   )

 (it "asdf"))
