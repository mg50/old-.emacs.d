USING: kernel locals ttt.core ;
IN: ttt.strategy

TUPLE: strategy ;

:: coord-valid? ( x y board -- ? ) [let board length :> l
    x 0 >= x max < and
    y 0 >= y max < and and
    x y board occupied? not and
]

: loop-until-valid-move ( board -- x y )
    until [
    

: ask-human-for-move ( board player -- x y ) >ui
    [ "Please make your move" print ]
    [   ]
    bi


GENERIC: >move-coords ( board player -- x y ) [ >ui ] keep

GENERIC: >move-coords ( player board -- ) swap dup >ui [ "Make your move" print ];
